%h1 Listing posts

%table
  %thead
    %tr
      %th Title
      %th Description
      %th Body
      %th
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.description
        %td= post.body
        %td= link_to 'Show', post
